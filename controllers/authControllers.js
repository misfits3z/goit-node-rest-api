import ctrlWrapper from "../helpers/ctrlWrapper.js";

export const registerController = ctrlWrapper(async (res, req) => {

    
})

// export const registerController = ctrlWrapper(async (res, req) => { });

// export const registerController = ctrlWrapper(async (res, req) => { });

// export const registerController = ctrlWrapper(async (res, req) => {});